/**
 * Movie Title Formatter
 *
 * You are cleaning inconsistent movie titles in a streaming metadata pipeline.
 *
 * Rules:
 *   - Remove leading/trailing spaces and collapse repeated internal spaces
 *   - Convert to Title Case
 *   - Small words should stay lowercase unless they are the first word:
 *     "and", "or", "the", "of", "in", "a", "an"
 *
 * Validation:
 *   - If input is not a string, return ""
 *   - If trimmed input is empty, return ""
 *
 * @param title
 * @returns {string}
 */
export function formatMovieTitle(title: string): string {
  // your code here
  return "";
}
