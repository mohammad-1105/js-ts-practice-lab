/**
 * Winter Recursion Workshop
 *
 * Create recursion-based helper functions for a winter craft app.
 * Loops are not allowed in intended solutions for this challenge.
 *
 * Functions:
 *
 * 1. `repeatSymbol(symbol, n)`
 *    - Repeat symbol recursively `n` times
 *    - Base case: `n <= 0` => `""`
 *    - Invalid symbol => `""`
 *
 * 2. `sumNestedValues(values)`
 *    - Sum all numbers in arbitrarily nested arrays
 *    - Skip non-number values
 *    - Invalid input => `0`
 *
 * 3. `flattenNestedValues(values)`
 *    - Flatten arbitrarily nested arrays into a single array
 *    - Invalid input => `[]`
 *
 * 4. `isMirrorPalindrome(text)`
 *    - Recursive palindrome check
 *    - Case-insensitive
 *    - Ignore non-alphanumeric characters
 *    - Invalid input => `false`
 *
 * 5. `generateTreePattern(levels)`
 *    - `1 => ["*"]`
 *    - `2 => ["*", "**", "*"]`
 *    - `3 => ["*", "**", "***", "**", "*"]`
 *    - Invalid levels => `[]`
 *
 * @hint Every recursive function needs a base case and a smaller recursive step.
 */
export function repeatSymbol(symbol: string, n: number): string {
  // your code here
  return "";
}

export function sumNestedValues(values: unknown): number {
  // your code here
  return 0;
}

export function flattenNestedValues(values: unknown): unknown[] {
  // your code here
  return [];
}

export function isMirrorPalindrome(text: unknown): boolean {
  // your code here
  return false;
}

export function generateTreePattern(levels: number): string[] {
  // your code here
  return [];
}
